name: manychat-run
on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  seo-build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Debug File Structure
        run: ls -R

      - name: Run Engine
        run: |
          mkdir -p manychat
          python manychat-gen.py

      - name: Push Updates
        run: |
          git config --global user.name "brightlane-bot"
          git config --global user.email "bot@brightlane.github.io"
          git add .
          git diff --staged --quiet || (git commit -m "2026 SEO Injection" && git push origin main)
